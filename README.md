# Backend Projesi (Staj Görevi)

Bu proje, Spring Boot kullanılarak geliştirilmiş, katmanlı mimariye uygun bir backend uygulamasıdır. İçerisinde veritabanı işlemleri, Docker yapılandırması ve REST API uç noktaları bulunmaktadır.

## 🛠️ Kullanılan Teknolojiler

- **Dil:** Java 17+
- **Framework:** Spring Boot 3.x
- **Veritabanı:** PostgreSQL
- **Konteynerleştirme:** Docker & Docker Compose
- **Build Aracı:** Maven

## 🚀 Projeyi Çalıştırma

Projenin kök dizininde `docker-compose.yml` dosyası bulunmaktadır. Aşağıdaki komut ile veritabanını ve uygulamayı ayağa kaldırabilirsiniz:

```bash
docker-compose up -d
```
